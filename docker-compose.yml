include:
    - docker/docker-mysql.yml

services:
    sqlscript:
        image: codepromaxtech/shopsuite:sql-master
        command: /bin/sh -c 'exit 0'
    shopsuite:
        image: codepromaxtech/shopsuite:master
        restart: always
        depends_on:
            - mysql
        ports:
            - "80:80"
        networks:
            - app_net
        volumes:
            - uploads:/app/public/uploads
            - logs:/app/writable/logs
        environment:
            - CI_ENVIRONMENT=production
            - FORCE_HTTPS=false
            - PHP_TIMEZONE=UTC
            - MYSQL_USERNAME=admin
            - MYSQL_PASSWORD=pointofsale
            - MYSQL_DB_NAME=shopsuite
            - MYSQL_HOST_NAME=mysql
